name: Collect Iraq Weekly News

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ai-token-usage-disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "AI token usage is disabled for this repository. Use sultjung/WR only."
